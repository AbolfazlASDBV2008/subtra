مترجم زیرنویس انیمه
ابزاری قدرتمند مبتنی بر وب برای ترجمه فایل‌های زیرنویس انیمه (VTT, SRT, ASS) به زبان فارسی روان با استفاده از هوش مصنوعی Gemini. این پروژه به صورت Client-Side اجرا می‌شود و خروجی نهایی را همیشه با فرمت ASS (با فونت فارسی ضمیمه‌شده) ارائه می‌دهد.
🚀 ویژگی‌های کلیدی
 * پشتیبانی از فرمت‌های محبوب: قابلیت خواندن و پردازش فایل‌های .srt، .vtt و .ass.
 * خروجی استاندارد ASS: تمام خروجی‌ها به فرمت .ass تبدیل می‌شوند و فونت فارسی وزیرمتن (Vazirmatn) به صورت خودکار درون فایل ضمیمه (Embed) می‌شود تا زیرنویس در تمام پلیرها به درستی نمایش داده شود.
 * حفظ کامل استایل (ویژه ASS): قابلیت منحصربه‌فرد برای ترجمه فایل‌های .ass با حفظ کامل تمام استایل‌ها، فونت‌ها، افکت‌ها و موقعیت‌بندی‌های اصلی.
 * تفکیک هوشمند محتوا: پروژه به صورت خودکار دیالوگ‌های عادی را از متن آهنگ‌ها (OP/ED) و نوشته‌های روماجی تشخیص می‌دهد و برای هر کدام، پرامپت تخصصی (محاوره‌ای برای دیالوگ، شاعرانه برای آهنگ) به Gemini ارسال می‌کند.
 * سیستم خود-اصلاح‌گر (Self-Correction):
   * اصلاح خطوط جا افتاده: پس از ترجمه اولیه، خطوطی که توسط هوش مصنوعی جا افتاده‌اند را شناسایی و مجدداً برای ترجمه ارسال می‌کند.
   * اصلاح ترجمه ناقص: خطوطی که حاوی کلمات انگلیسی باقی‌مانده، کاراکترهای خراب یا اسکریپت‌های زبانی غیرفارسی هستند را شناسایی و برای بازنویسی و اصلاح ارسال می‌کند.
 * اتصال به Gemini: از API رسمی گوگل Gemini (مدل‌های 2.5 Pro و 2.5 Flash) از طریق streamGenerateContent برای دریافت پاسخ لحظه‌ای استفاده می‌کند.
 * تنظیمات پیشرفته:
   * قابلیت ویرایش کامل پرامپت سیستم (System Prompt).
   * تنظیم دستی نرخ فریم (FPS) برای هماهنگ‌سازی زمان‌بندی.
   * گزینه پراکسی داخلی (توسط Worker) برای دور زدن تحریم‌ها و دسترسی آسان‌تر به API.
   * قابلیت غیرفعال کردن فیلترهای ایمنی (Safety Settings) Gemini.
 * پردازش در سمت کاربر: تمام فرآیندها (به جز تماس با API) در مرورگر کاربر انجام می‌شود. کلید API شما هرگز در سروری ذخیره نمی‌شود و مستقیماً به گوگل (یا پراکسی) ارسال می‌گردد.
 * رابط کاربری مدرن:
   * طراحی شده با Tailwind CSS در حالت تاریک (Dark Mode).
   * ناحیه کشیدن و رها کردن (Drag & Drop) برای آپلود فایل.
   * نمایش زنده و لحظه‌ای فرآیند ترجمه (liveOutput).
   * گزارش (Log) دقیق از تمام مراحل عملیات.
🔧 نحوه استفاده
 * ورود کلید API: در بخش «تنظیمات»، کلید API گوگل (Gemini) خود را وارد کنید.
 * تنظیمات (اختیاری): مدل هوش مصنوعی، FPS یا پرامپت سیستم را در صورت نیاز سفارشی کنید.
 * آپلود فایل: فایل(های) زیرنویس خود را در کادر مشخص شده بکشید و رها کنید.
 * انتخاب فرمت (مخصوص ASS): اگر فایل .ass آپلود کرده‌اید، می‌توانید بین «حفظ استایل‌ها» (حرفه‌ای) یا «تبدیل به SRT» (ساده) یکی را انتخاب کنید.
 * شروع ترجمه: روی دکمه «شروع ترجمه» کلیک کنید.
 * نظارت بر فرآیند: پیشرفت عملیات را از طریق نوار پیشرفت، خروجی زنده و لاگ وضعیت دنبال کنید.
 * دانلود: پس از اتمام موفقیت‌آمیز، فایل(های) .ass ترجمه‌شده و آماده را دانلود کنید.
💻 تکنولوژی‌های استفاده شده
 * فرانت‌اند: HTML5, Tailwind CSS, JavaScript (ES6 Modules)
 * API: Google Gemini API (v1beta)
👤 توسعه‌دهنده
 * Abolfazl_ASDBV
 * تلگرام: anime_sub_Persian
 * اینستاگرام: anime_wd20