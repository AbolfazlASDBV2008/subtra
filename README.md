## 🌟 مترجم زیرنویس انیمه - ترجمه روان و خودکار با هوش مصنوعی Gemini 🤖

این یک ابزار سمت-کاربر (Client-Side) است که با استفاده از **Google Gemini API**، به شما امکان می‌دهد فایل‌های زیرنویس انیمه (VTT، SRT، ASS) خود را با حفظ کامل زمان‌بندی و ساختارهای فنی، به **فارسی روان و عامیانه** ترجمه کنید. این ابزار بهینه‌سازی شده برای دور زدن محدودیت‌های جغرافیایی و ارائه ترجمه‌ای با کیفیت بومی‌سازی شده است.

-----

### ✨ قابلیت‌های کلیدی

  * **موتور قدرتمند Gemini:** ترجمه زیرنویس‌ها با استفاده از مدل‌های پیشرفته Google Gemini (مانند **Gemini 2.5 Pro** برای دقت بالا و **Gemini 2.5 Flash** برای سرعت بیشتر).
  * **بومی‌سازی روان:** استفاده از یک **پرامپت سیستم** تخصصی برای تولید ترجمه‌هایی که کاملاً طبیعی، عامیانه و وفادار به لحن اصلی انیمه باشند.
  * **حفظ ساختار فنی:** نگهداری دقیق فریم‌های زمانی MicroDVD و تگ‌های استایل (مانند `|` و `{}`) برای اطمینان از سازگاری کامل با فایل‌های ASS خروجی.
  * **قابلیت خود-اصلاح‌گری (Self-Correction):** بررسی و اصلاح خودکار خطوطی که به دلیل خطای API یا مدل، ناقص، ترکیبی (فارسی و انگلیسی) یا شامل کاراکترهای خراب شده‌اند. همچنین تلاش مجدد برای ترجمه خطوط جا افتاده.
  * **پشتیبانی از پروکسی و تنظیمات امنیتی:** امکان استفاده از پراکسی (Proxy) برای دور زدن تحریم‌ها و همچنین غیرفعال‌سازی فیلترهای محتوایی Gemini (Safety Settings) در صورت نیاز.
  * **خروجی ASS استاندارد:** دریافت خروجی نهایی با فرمت **ASS** با تنظیمات پیش‌فرض برای نمایش بهتر فونت فارسی (Vazirmatn).

-----

### 🛠️ راهنمای شروع به کار

#### ۱. دریافت کلید API گوگل (Gemini API)

برای استفاده از این ابزار، نیاز به یک کلید API معتبر از گوگل دارید. این کلید برای ارتباط با مدل‌های هوش مصنوعی Gemini ضروری است:

1.  به وبسایت **Google AI Studio** یا **Google AI Developer** بروید.
2.  یک حساب کاربری ایجاد یا وارد شوید.
3.  یک کلید جدید API تولید کنید.
4.  **توجه:** در زمان استفاده از این ابزار، کلید API شما فقط در مرورگر شما ذخیره و استفاده می‌شود و به هیچ سرور دیگری ارسال نمی‌گردد.

#### ۲. تنظیمات در ابزار

1.  فایل `index.html` را در مرورگر خود باز کنید.
2.  **کلید API گوگل (Gemini):** کلید API که در مرحله قبل دریافت کردید را در قسمت مربوطه وارد کنید.
3.  **انتخاب مدل هوش مصنوعی:**
      * **`Gemini 2.5 Pro`**: توصیه می‌شود (دقیق‌ترین ترجمه).
      * **`Gemini 2.5 Flash`**: سریع‌تر اما ممکن است کمی دقت کمتری داشته باشد.
4.  **استفاده از پراکسی (اختیاری):** اگر با خطای دسترسی مواجه شدید (به دلیل محدودیت‌های جغرافیایی)، این گزینه را فعال کنید.
5.  **ذخیره تنظیمات:** پس از وارد کردن کلید و انتخاب‌های خود، دکمه **"ذخیره تنظیمات"** را فشار دهید.

#### ۳. ترجمه فایل‌ها

1.  فایل‌های زیرنویس (با فرمت‌های **.vtt**، **.srt** یا **.ass**) خود را در ناحیه **"انتخاب فایل‌ها"** بکشید و رها کنید یا با کلیک کردن آن‌ها را انتخاب کنید.
2.  دکمه **"شروع ترجمه"** را فشار دهید.
3.  ابزار فایل‌ها را یک به یک آپلود، ترجمه و اصلاح می‌کند. می‌توانید پیشرفت و خروجی زنده را در بخش وضعیت مشاهده کنید.
4.  پس از اتمام عملیات، دکمه **"دانلود فایل(ها)"** فعال می‌شود و می‌توانید زیرنویس‌های فارسی‌شده با فرمت **ASS** را دانلود نمایید.

-----

### 💡 پرامپت سیستم پیش‌فرض

ابزار از پرامپت زیر برای تضمین کیفیت ترجمه استفاده می‌کند. شما می‌توانید آن را در بخش تنظیمات ویرایش کنید:

```
## مأموریت اصلی
شما یک دستیار هوش مصنوعی متخصص در بومی‌سازی زیرنویس انیمه به زبان فارسی هستید. وظیفه شما ارائه یک خروجی روان، جذاب و عامیانه است که در عین حال به شدت به مفهوم و محتوای متن اصلی وفادار باشد.

## اصول و قوانین قاطع ترجمه
1.  **ترجمه عامیانه و روان (ضد-رباتیک):** ترجمه باید کاملاً طبیعی و به دور از حس ماشینی یا خشک باشد. از زبان فارسی امروزی، پویا و در مواقع لزوم، عامیانه و گفتاری استفاده کنید.
2.  **وفاداری به مفهوم، نه کلمات:** برگرداندن *معنی* و *لحن* دیالوگ اولویت دارد. از ترجمه کلمه به کلمه که منجر به عبارات نامأنوس فارسی می‌شود، پرهیز کنید.
3.  **قانون عدم افزودن یا حذف:** در حین روان‌سازی، به هیچ وجه اطلاعات، مفاهیم یا دیالوگ‌هایی که در متن اصلی وجود ندارند را **اضافه نکنید**. همچنین هیچ بخشی از متن اصلی را حذف یا سانسور نکنید. ترجمه باید کامل و بی پرده باشد.
4.  **انسجام و شخصیت:** لحن هر کاراکتر (رسمی، دوستانه، طنزآمیز) و اصطلاحات کلیدی باید در سرتاسر متن یکسان و با انسجام ترجمه شوند.
5.  **حفظ کامل ساختار فنی:** کاراکترهای پایپ‌لاین (`|`) برای شکستن خطوط و آکولادها (`{` و `}`) باید **بدون هیچ تغییری و دقیقاً در همان موقعیت** در متن ترجمه‌شده نیز حفظ شوند.

## فرمت ورودی و خروجی
شما یک فایل متنی حاوی دیالوگ‌ها (با فرمت microdvd) دریافت خواهید کرد. شما باید *دقیقا* همان فایل را با حفظ کامل زمان‌بندی (فریم‌ها) و جایگزینی متن انگلیسی با ترجمه فارسی روان و وفادار برگردانید.
```
